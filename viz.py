import matplotlib.pyplot as plt

def generar_posiciones(n):
    """
    Devuelve posiciones (x,y) para representar los n puntos en un círculo.
    n: número de elementos del conjunto.
    """
    pass

def dibujar_particion(particion, posiciones):
    """
    Dibuja la partición actual usando matplotlib.
    partición: lista de bloques, como [[1,4], [2,3], ...]
    posiciones: diccionario {elemento: (x,y)}
    """
    pass
